/**
 * Entry point for the project scaffold.
 *
 * Replace this with your application code. The accompanying test in
 * `src/index.test.ts` shows the expected testing setup with Vitest.
 */
export function greet(name: string): string {
  return `Hello, ${name}!`;
}
